<%- include("partials/header", { title: "Dashboard" }) %>

<section class="max-w-6xl mx-auto mt-12 p-6 bg-white shadow-lg rounded-2xl space-y-10">
  <h1 class="text-3xl font-bold text-indigo-700">🎛️ Admin Dashboard</h1>

  <!-- Stats Cards -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    <div class="bg-indigo-100 p-6 rounded-xl shadow hover:shadow-md transition">
      <div class="text-indigo-600 text-xl font-semibold mb-1">🎸 Total Items</div>
      <div class="text-4xl font-bold text-indigo-800"><%= stats.items %></div>
    </div>
    <div class="bg-yellow-100 p-6 rounded-xl shadow hover:shadow-md transition">
      <div class="text-yellow-600 text-xl font-semibold mb-1">📁 Categories</div>
      <div class="text-4xl font-bold text-yellow-800"><%= stats.categories %></div>
    </div>
    <div class="bg-green-100 p-6 rounded-xl shadow hover:shadow-md transition">
      <div class="text-green-600 text-xl font-semibold mb-1">📦 Total Quantity</div>
      <div class="text-4xl font-bold text-green-800"><%= stats.quantity %></div>
    </div>
    <div class="bg-pink-100 p-6 rounded-xl shadow hover:shadow-md transition">
      <div class="text-pink-600 text-xl font-semibold mb-1">💰 Avg. Price</div>
      <div class="text-4xl font-bold text-pink-800">৳ <%= stats.averagePrice %></div>
    </div>
  </div>

  <!-- Admin Tips & Motivation -->
  <div class="p-5 bg-indigo-50 rounded-xl shadow flex items-center justify-between">
    <div>
      <h2 class="text-lg font-semibold text-indigo-800">💡 Tip of the Day</h2>
      <p class="text-sm text-gray-600 mt-1">
        “Consistency beats intensity. Keep the database clean and updated regularly.”
      </p>
    </div>
    <div class="text-4xl">✨</div>
  </div>

  <!-- Quick Links -->
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
    <div class="bg-white border border-gray-200 rounded-xl p-4 shadow-sm">
      <h3 class="text-md font-semibold text-gray-700 mb-2">⚡ Quick Actions</h3>
      <ul class="space-y-2 text-sm text-indigo-600">
        <li><a href="/items/new" class="hover:text-yellow-300">➕ Add New Item</a></li>
        <li><a href="/categories/new" class="hover:text-yellow-300">📂 Create New Category</a></li>
        <li><a href="/items" class="hover:text-yellow-300">🎹 Manage Instruments</a></li>
        <li><a href="/categories" class="hover:text-yellow-300">📁 Manage Categories</a></li>
      </ul>
    </div>

    <!-- Recent "Activity" -->
    <div x-data="{ open: true }" class="bg-white border border-gray-200 rounded-xl p-4 shadow-sm">
      <div class="flex justify-between items-center cursor-pointer" @click="open = !open">
        <h3 class="text-md font-semibold text-gray-700">🕓 Recent Activity</h3>
        <svg :class="{'rotate-180': !open}" class="w-5 h-5 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </div>
      <div x-show="open" x-transition class="mt-3 text-sm text-gray-700 space-y-1">
        <div>✔️ Edited <strong>Guitar</strong> item.</div>
        <div>🆕 Created <strong>Wind Instruments</strong> category.</div>
        <div>🗑️ Deleted item <strong>Old Tabla</strong>.</div>
        <div>✔️ Updated quantity for <strong>Yamaha Piano</strong>.</div>
        <div>🆕 Added <strong>Ukulele</strong>.</div>
      </div>
    </div>
  </div>

  <p class="text-xs text-gray-400 text-right">📅 Last updated: <%= new Date().toLocaleString() %></p>
</section>

<script src="https://unpkg.com/alpinejs" defer></script>
<%- include("partials/footer") %>
