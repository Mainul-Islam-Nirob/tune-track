<%- include('../partials/header') %>

<main class="max-w-3xl mx-auto mt-12 p-4 sm:p-6">
  <section class="bg-white rounded-2xl shadow-lg p-6 sm:p-10">
    <h1 class="text-3xl font-bold text-indigo-700 mb-2 text-center">Add New Instrument Category</h1>

    <form action="/categories/new" method="POST" enctype="multipart/form-data" class="space-y-6">
      <!-- Name -->
      <div>
        <label for="name" class="block text-sm font-semibold text-gray-700 mb-1">Category Name <span class="text-red-500">*</span></label>
        <input
          type="text"
          id="name"
          name="name"
          value="<%= category.name || '' %>"
          placeholder="e.g. Strings, Percussion"
          class="w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:outline-none shadow-sm transition"
          required
        />
      </div>

      <!-- Description -->
      <div>
        <label for="description" class="block text-sm font-semibold text-gray-700 mb-1">Description</label>
        <textarea
          id="description"
          name="description"
          rows="4"
          placeholder="Brief description of this category..."
          class="w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:outline-none shadow-sm transition resize-none"
        ><%= category.description || '' %></textarea>
      </div>

      <!-- Image Upload -->
      <div>
        <label for="image" class="block text-sm font-semibold text-gray-700 mb-1">Category Image <span class="text-red-500">*</span></label>
        <input
          type="file"
          id="image"
          name="image"
          accept="image/*"
          class="w-full px-4 py-2 border border-gray-300 rounded-xl bg-white text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none shadow-sm transition"
        />
        <p class="text-xs text-gray-500 mt-1">Accepted: JPG, PNG. Max size: 2MB.</p>
      </div>

      <!-- Buttons -->
      <div class="flex justify-between items-center pt-2">
        <button
          type="submit"
          class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold px-6 py-2.5 rounded-xl shadow transition"
        >
          💾 Save Category
        </button>
        <a href="/categories" class="text-sm text-gray-500 hover:text-indigo-600 transition">← Cancel</a>
      </div>
    </form>

    <!-- Optional: Image Preview -->
    <!-- <img id="preview" class="hidden mt-6 rounded-xl w-full max-w-sm border shadow" /> -->
  </section>
</main>

<!-- <script>
  const input = document.getElementById('image');
  input?.addEventListener('change', function () {
    const preview = document.getElementById('preview');
    const file = this.files[0];
    if (file) {
      const reader = new FileReader();
      reader.onload = () => {
        preview.src = reader.result;
        preview.classList.remove('hidden');
      };
      reader.readAsDataURL(file);
    }
  });
</script> -->

<%- include('../partials/footer') %>
